# 约瑟夫环问题实验报告

## 一、实验目的

1. 掌握线性表及其顺序存储与链式存储结构的概念。
2. 掌握两种存储方式的基本运算、实现方法和技术。
3. 灵活应用线性表进行程序设计，解决实际问题。

## 二、实验内容

约瑟夫环求解。

编号为 1，2，...，N 的 N 个人按顺时针方向围坐一圈，每人持有一个密码（正整数）。一开始任选一个正整数作为报数上限值 M，从第一个人开始按顺时针方向自 1 开始顺序报数，报到 M 时停止报数。报 M 的人出列，将他的密码作为新的 M 值，从他在顺时针方向上的下一个人开始重新从 1 报数，如此下去，直至所有人全部出列为止。试设计一个程序求出出列顺序。利用单向循环链表存储结构模拟此过程，按照出列的顺序打印出每个人的编号。

## 三、算法思想

### 3.1 数据结构设计

使用**单向循环链表**存储 N 个人的信息，每个节点包含：
- `id`：编号（1 到 N）
- `password`：密码（正整数）
- `next`：指向下一个节点的指针

```
struct Node {
    int id;         // 编号
    int password;   // 密码
    Node* next;     // 指向下一个节点
};
```

### 3.2 算法步骤

1. **创建循环链表**：根据输入的人数 N 和每个人的密码，创建一个包含 N 个节点的单向循环链表，尾节点的 next 指向头节点。

2. **初始化**：设置当前指针 current 指向头节点，前驱指针 prev 指向尾节点，初始报数上限值为 M。

3. **模拟报数过程**：
   - 从当前节点开始报数，报数 M-1 次后，current 指向报到 M 的人
   - 输出该人的编号
   - 将该人的密码设为新的 M 值
   - 删除该节点（prev->next = current->next）
   - current 移动到下一个节点
   - 重复上述步骤，直到所有人都出列

4. **结束**：当链表为空时，所有人都已出列，程序结束。

### 3.3 时间复杂度分析

- 创建链表：O(N)
- 模拟过程：每次出列需要报数 M 次，共出列 N 人，总时间复杂度为 O(N × M)

### 3.4 空间复杂度分析

- 需要 N 个节点存储所有人的信息，空间复杂度为 O(N)

## 四、程序源代码

```cpp
/*
 * 约瑟夫环问题 (Josephus Problem)
 * 
 * 算法思想：
 * 1. 使用单向循环链表存储N个人的信息，每个节点包含编号和密码
 * 2. 从第一个人开始，按顺时针方向依次报数
 * 3. 报到M时，该人出列，将其密码作为新的M值
 * 4. 从出列者的下一个人继续报数
 * 5. 重复步骤3-4，直到所有人都出列
 * 
 * 数据结构：
 * - 使用单向循环链表，不需要头结点
 * - 每个节点包含：编号(id)、密码(password)、指向下一节点的指针(next)
 */

#include <iostream>
using namespace std;

// 定义链表节点结构
struct Node {
    int id;         // 编号
    int password;   // 密码
    Node* next;     // 指向下一个节点
};

// 创建循环链表
Node* createCircularList(int n, int passwords[]) {
    if (n <= 0) return nullptr;  // 空表处理
    
    Node* head = nullptr;
    Node* tail = nullptr;
    
    for (int i = 0; i < n; i++) {
        Node* newNode = new Node;
        newNode->id = i + 1;            // 编号从1开始
        newNode->password = passwords[i];
        newNode->next = nullptr;
        
        if (head == nullptr) {
            head = newNode;
            tail = newNode;
        } else {
            tail->next = newNode;
            tail = newNode;
        }
    }
    
    // 形成循环，尾节点指向头节点
    if (tail != nullptr) {
        tail->next = head;
    }
    
    return head;
}

// 约瑟夫环求解
void josephus(int n, int m, int passwords[]) {
    if (n <= 0) {
        cout << "人数必须大于0！" << endl;
        return;
    }
    
    // 创建循环链表
    Node* head = createCircularList(n, passwords);
    if (head == nullptr) return;
    
    Node* current = head;
    Node* prev = nullptr;
    
    // 找到尾节点（作为初始的prev）
    Node* temp = head;
    while (temp->next != head) {
        temp = temp->next;
    }
    prev = temp;
    
    cout << "出列顺序为：";
    
    int count = n;
    bool first = true;
    
    while (count > 0) {
        // 报数m-1次（因为当前节点算第1个）
        for (int i = 1; i < m; i++) {
            prev = current;
            current = current->next;
        }
        
        // 输出当前出列者的编号
        if (!first) {
            cout << "，";
        }
        cout << current->id;
        first = false;
        
        // 保存密码作为新的M值
        m = current->password;
        
        // 删除当前节点
        Node* toDelete = current;
        prev->next = current->next;
        current = current->next;
        
        delete toDelete;
        count--;
    }
    
    cout << endl;
}

int main() {
    int n, m;
    
    cout << "========== 约瑟夫环问题求解 ==========" << endl;
    cout << endl;
    
    cout << "请输入初始报数上限值M：";
    cin >> m;
    
    cout << "请输入总人数N（N<=30）：";
    cin >> n;
    
    if (n <= 0 || n > 30) {
        cout << "人数必须在1到30之间！" << endl;
        return 1;
    }
    
    if (m <= 0) {
        cout << "报数上限值必须为正整数！" << endl;
        return 1;
    }
    
    int* passwords = new int[n];
    
    cout << "请依次输入" << n << "个人的密码（正整数）：" << endl;
    for (int i = 0; i < n; i++) {
        cout << "第" << (i + 1) << "个人的密码：";
        cin >> passwords[i];
        if (passwords[i] <= 0) {
            cout << "密码必须为正整数！" << endl;
            delete[] passwords;
            return 1;
        }
    }
    
    cout << endl;
    josephus(n, m, passwords);
    
    delete[] passwords;
    
    return 0;
}
```

## 五、运行结果

### 5.1 测试数据

- 初始报数上限值 M = 20
- 总人数 N = 7
- 7 个人的密码依次为：3, 1, 7, 2, 4, 8, 4

### 5.2 运行截图

```
========== 约瑟夫环问题求解 ==========

请输入初始报数上限值M：20
请输入总人数N（N<=30）：7
请依次输入7个人的密码（正整数）：
第1个人的密码：3
第2个人的密码：1
第3个人的密码：7
第4个人的密码：2
第5个人的密码：4
第6个人的密码：8
第7个人的密码：4

出列顺序为：6，1，4，7，2，3，5
```

### 5.3 结果验证

输出的出列顺序为 **6，1，4，7，2，3，5**，与预期结果完全一致。

**验证过程**：
1. 初始 M=20，从1号开始报数，20 mod 7 = 6，所以6号出列，新 M=8
2. 从7号开始报数，数8个：7,1,2,3,4,5,7,1，1号出列，新 M=3
3. 从2号开始报数，数3个：2,3,4，4号出列，新 M=2
4. 从5号开始报数，数2个：5,7，7号出列，新 M=4
5. 从2号开始报数，数4个：2,3,5,2，2号出列，新 M=1
6. 从3号开始报数，数1个：3，3号出列，新 M=7
7. 只剩5号，5号出列

## 六、遇到的问题及解决办法

### 6.1 问题1：循环链表的创建

**问题描述**：初次实现时忘记将尾节点的 next 指向头节点，导致程序在遍历时出现错误。

**解决办法**：在创建链表的最后一步，确保将尾节点的 next 指针指向头节点，形成循环结构：
```cpp
if (tail != nullptr) {
    tail->next = head;
}
```

### 6.2 问题2：删除节点时的指针处理

**问题描述**：删除节点后，如果直接使用 current = current->next，会导致访问已释放的内存。

**解决办法**：先保存要删除的节点指针，更新 current 后再释放内存：
```cpp
Node* toDelete = current;
prev->next = current->next;
current = current->next;
delete toDelete;
```

### 6.3 问题3：空表和只有一个节点的特殊情况

**问题描述**：当 N=0 或只有一个人时，程序需要特殊处理。

**解决办法**：在函数开头添加空表判断，对于只有一个人的情况，循环链表自然能够处理。

## 七、实验总结

通过本次实验，我深入理解了以下内容：

1. **循环链表的结构特点**：循环链表的最后一个节点指向第一个节点，形成环形结构，适合处理需要循环遍历的问题。

2. **单向循环链表的操作**：掌握了创建、遍历、删除等基本操作，特别是删除节点时需要维护前驱指针。

3. **约瑟夫问题的本质**：这是一个典型的模拟问题，使用循环链表可以自然地模拟围坐一圈的场景。

4. **动态内存管理**：使用 new 分配内存后，需要使用 delete 释放，避免内存泄漏。

5. **边界条件处理**：程序设计时需要考虑各种边界情况，如空表、单节点等。

本实验加深了我对线性表链式存储结构的理解，提高了使用链表解决实际问题的能力。
